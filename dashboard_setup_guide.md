# Clear Maker 2C: 生徒・管理者ダッシュボード作成ガイド

Clear Maker 2B と同様に、個別トークン（`?token=...`）を使って生徒一人ひとりが自分の学習記録（到達度、カレンダー、履歴）を確認できるダッシュボードの導入手順です。

---

## 1. 全体構成

| ファイル名 | 種類 | 説明 |
| :--- | :--- | :--- |
| `student_dashboard.gs` | スクリプト (`.gs`) | バックエンド集計・トークン認証・トークン発行処理 |
| `student_dashboard.html` | HTML (`.html`) | 生徒画面・管理者画面のUI（カレンダー・進捗ゲージなど） |

---

## 2. GASプロジェクトの作成とファイル配置

1. [Google Apps Script（新規プロジェクト）](https://script.new) を開きます。
2. プロジェクト名を `Clear Maker 2C Dashboard` に変更します。
3. 以下の2つのファイルを配置します：

### ① `コード.gs`（スクリプト）
最初からある `コード.gs` の内容を消し、[`student_dashboard.gs`](file:///C:/projects/clear%20maker2C/student_dashboard.gs) の内容をコピー＆ペーストします。
※ スプレッドシートID（`1pB5CXfzdF6vWD55pbily0ZIf3Oteb-gdmVs8U9dGGf4`）が指定されていることを確認してください。

### ② `student_dashboard.html`（HTML）
左メニューの「＋」→「HTML」をクリックし、ファイル名を `student_dashboard` と入力します。
作成されたエディタに [`student_dashboard.html`](file:///C:/projects/clear%20maker2C/student_dashboard.html) の内容をコピー＆ペーストします。

---

## 3. 初期管理者トークンの発行

管理者自身がダッシュボード（全生徒一覧やトークン管理）を開くためのトークンを発行します。

1. GASエディタ上部の関数選択プルダウンから **`generateAdminToken`** を選択します。
2. **「実行」** ボタンをクリックします。
3. （初回は「アクセス承認」が求められるので、アカウントを選択して許可してください）
4. スプレッドシート（`Clear Maker 2C 進捗管理`）を開くと、**「公開トークン」** というシートが自動作成され、1行目に管理者用トークンが生成されています。

---

## 4. Webアプリとしてデプロイ

1. 右上の **「デプロイ」** → **「新しいデプロイ」** をクリックします。
2. 種類の選択（歯車アイコン）で **「ウェブアプリ」** を選択します。
3. 設定項目：
   - **説明**: `Clear Maker 2C Dashboard v1`
   - **次のユーザーとして実行**: **`自分`**
   - **アクセスできるユーザー**: **`全員`**
4. **「デプロイ」** をクリックし、発行された **ウェブアプリURL** をコピーします。

---

## 5. ダッシュボードの利用方法

### 👑 管理者として開く
発行されたWebアプリURLの末尾に、ステップ3で発行した管理者トークンを付けてブラウザで開きます。

```text
https://script.google.com/macros/s/AKfy.../exec?token=【管理者トークン】
```

- 全生徒の進捗サマリーが表示されます。
- **「未発行トークンを一括作成」** ボタンを押すと、テストをクリアした全生徒の個別トークンが一度に発行されます。
- 生徒一覧の **「URLコピー」** ボタンを押すと、その生徒専用の個別ダッシュボードURLがクリップボードにコピーされます。

---

### 🎓 生徒に個別URLを案内する（スプレッドシートで確認）
生徒配布用のURLは、スプレッドシートの **「公開トークン」シート（I列）** および **「英単語進捗」シート（H列）** に自動出力されます！

- スプレッドシートから各生徒のURL（`https://script.google.com/.../exec?token=xxx`）をコピーしてメールやチャット、個票等で生徒へ配布できます。
- 生徒がそのリンクを開くと、**本人だけの進捗・称号・学習カレンダー・合格履歴** が表示されます。
- 生徒トークンでは他人のデータや管理者機能は見えません。

---

## 6. スプレッドシート上のURLを一括更新・最新化したい場合

デプロイ後やWebアプリURLを変更した際に、スプレッドシートに記載されたURLを一括で最新化したい場合は：
1. GASエディタの関数プルダウンから **`updateAllDashboardUrlsInSheet`** を選択して実行します。
2. 「公開トークン」シート（I列）および「英単語進捗」シート（H列）の全生徒のダッシュボードURLが自動で更新されます。
